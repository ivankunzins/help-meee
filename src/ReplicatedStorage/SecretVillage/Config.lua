-- Central tuning for Secret Village. Keep gameplay numbers here.
local Config = {
    RoundSeconds = 30 * 60,
    StartingMoney = 250,
    HintCost = 500,
    JanitorCost = 100,
    FisherCost = 250,
    LeafReward = 1,
    FishReward = 8,
    FoodSellerHint = "Ищи воду, но смотри не на поверхность — секрет спрятан ниже.",
    MaxSecrets = 100,
    DailyGoal = 3,
}
return Config
